[workspace]
members = ["core-engine"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/example/nextgen-editor"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
